# model weights & large / regenerable artifacts — never commit
models/
*.safetensors
*.npz
*.mlpackage/

# python
__pycache__/
*.pyc

# swift / xcode build
.build/
pf/.build/
.swiftpm/
*.xcodeproj
*.profraw

# generated eval corpora (regenerate via make_gold*.py / make_gold_ai4privacy.py)
reference/gold_*.jsonl

# Claude Code session / hook state — never commit
.claude/

.DS_Store
